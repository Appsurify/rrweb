{
  "name": "@booking-demo/selenium-node-test",
  "version": "0.0.0",
  "private": true,
  "description": "Consumer demo: Node.js node:test + Selenium v4 + @appsurify-testmap/rrweb-selenium-plugin.",
  "scripts": {
    "pretest": "node -e \"require('fs').rmSync('test-results',{recursive:true,force:true})\"",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@appsurify-testmap/rrweb-selenium-plugin": "file:../../../../packages/rrweb-selenium-plugin"
  },
  "devDependencies": {
    "selenium-webdriver": "^4.27.0"
  }
}
